<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/newsListFragment">

    <fragment
        android:id="@+id/newsListFragment"
        android:name="com.novikon.app.ui.newslist.NewsListFragment"
        android:label="@string/app_name"
        tools:layout="@layout/fragment_news_list">
        <action
            android:id="@+id/action_newsListFragment_to_detailFragment"
            app:destination="@id/detailFragment" />
    </fragment>

    <fragment
        android:id="@+id/detailFragment"
        android:name="com.novikon.app.ui.detail.DetailFragment"
        android:label="Детали новости"
        tools:layout="@layout/fragment_detail">
        <argument
            android:name="newsItem"
            app:argType="com.novikon.app.data.model.NewsItem" />
    </fragment>

</navigation>
